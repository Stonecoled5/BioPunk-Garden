local struct = {}
struct.info = "This is the shed. This is where the robots return to every night and pick up and exchance items. Items replenish every turn to their max."
struct.pic = hash("shed_first_version")
return struct